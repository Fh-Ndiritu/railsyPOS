<div class="item-link gap-4" id="<%= dom_id category %>">
  <%= render(ItemThumbnailComponent.new(
        title: category.name,
        count: category.products_count,
        classes: '',
        href: href,
        image_url: category.image.url,
        data: { turbo_frame: 'products'}
        )
        ) %>
</div>