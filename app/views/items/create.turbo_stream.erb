<%# if @item.saved_change_to_quantity? %>
<%#= turbo_stream.replace dom_id(@item), partial: "items/item", locals: { item: @item } %>
<%# else %>
<%#= turbo_stream.append "order_items", partial: "items/item", locals: { item: @item } %>
<%# end %>
<%#= turbo_stream.replace dom_id(@item.product), partial: "orders/option", locals: { product: @item.product, order: @item.order } %>
<!---->